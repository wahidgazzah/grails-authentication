package com.dihaw.usermanager

import javax.net.ssl.SSLEngineResult.Status;

/*
class QuestionTagLib {
	def getPartialName = { attrs, body ->
		def question = attrs["question"]
		println"questio : " + question
		
		if(question.type == "Question : oui/non") {
			out << g.render(template:"/quizz/yesno", model : [question: question])
			
		} else if(question.type == "Question : oui/non avec description"){
			out << g.render(template:"/quizz/yesnowithdescription", model : [question: question])
		}
		
		 else if(question.type == "Question : choix unique"){
			out << g.render(template:"/quizz/uniqchoice", model : [question: question])
		}
		 
		else if(question.type == "Question ouverte : champs texte"){
			out << g.render(template:"/quizz/textfield", model : [question: question])
		}
		else if(question.type == "Question : choix multiple"){
			out << g.render(template:"/quizz/multichoice", model : [question: question])
		}
		else if(question.type == "question ouverte"){
			
			out << g.render(template:"/quizz/textfield", model : [question: question])
		}
	}
	
}
*/